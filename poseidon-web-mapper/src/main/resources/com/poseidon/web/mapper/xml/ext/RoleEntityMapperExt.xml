<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.poseidon.web.mapper.ext.RoleEntityMapperExt">
  <resultMap id="BaseResultMap" type="com.poseidon.model.RoleEntity">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Fri Apr 21 14:33:25 CST 2017.
    -->
    <id column="role_id" jdbcType="VARCHAR" property="roleId" />
    <result column="role_name" jdbcType="VARCHAR" property="roleName" />
    <result column="seq" jdbcType="TINYINT" property="seq" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="role_status" jdbcType="TINYINT" property="roleStatus" />
  </resultMap>

  <select id="selectUserRolesByUserId" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
        r.role_id,
        r.role_name,
        r.seq,
        r.description,
        r.role_status
    FROM
        role r
    RIGHT JOIN user_role ur ON ur.role_id = r.role_id
    RIGHT JOIN b2c_user u ON u.user_id = ur.user_id
    WHERE
        u.user_id = #{userId,jdbcType=VARCHAR}
  </select>
  <select id="selectRoleById" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    role_id, role_name, seq, description, role_status
    FROM
    role
    WHERE
    role_status = 0 AND role_id = #{roleId,jdbcType=VARCHAR}
  </select>
  <select id="getAllRole" resultMap="BaseResultMap">
    SELECT
      role_id, role_name, seq, description, role_status
    FROM
    role
    WHERE
    role_status = 0
  </select>

  <delete id="deleteRoleResourcesByRoleId" parameterType="java.lang.String">
    delete from role_resource
    where role_id = #{roleId,jdbcType=VARCHAR}
  </delete>

  <insert id="insertRoleResources" parameterType="java.lang.String">
    insert into role_resource (role_resource_id, role_id, resource_id)
    values (#{uuid,jdbcType=VARCHAR}, #{roleId,jdbcType=VARCHAR}, #{resourceId,jdbcType=VARCHAR})
  </insert>

  <update id="enable" parameterType="java.lang.String">
    update role
    set role_status = 1
    where role_id = #{roleId,jdbcType=VARCHAR}
  </update>

  <update id="disable" parameterType="java.lang.String">
    update role
    set role_status = 0
    where role_id = #{roleId,jdbcType=VARCHAR}
  </update>

</mapper>